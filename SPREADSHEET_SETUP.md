# 劇団ハレトケ チケット管理システム（スプレッドシート＆GAS連携）設定マニュアル

Googleスプレッドシートをチケット管理データベースとして使用するためのセットアップマニュアルです。
チケットごとの個別管理・n枚単位の一部キャンセル機能・メール文面カスタマイズを含めた完全版マニュアルです。

---

## 🚀 セットアップ手順（初回のみ）

### STEP 1: Googleスプレッドシートを作成する
1. Googleドライブまたはブラウザで [Googleスプレッドシート](https://sheets.new) を新規作成します。
2. スプレッドシートのタイトルを例：`劇団ハレトケ_チケット予約管理` に変更します。

---

### STEP 2: GAS（Apps Script）にコードを貼り付ける
1. スプレッドシート上部メニューの **「拡張機能」 > 「Apps Script」** をクリックします。
2. 開いたエディタ内の `function myFunction() {...}` をすべて削除し、プロジェクト内の [gas_ticket_code.js](file:///Users/minayamano/Haretoke--web/gas_ticket_code.js) の内容をすべてコピー＆ペーストします。
3. 💾 アイコン（または `Ctrl + S` / `Cmd + S`）を押して保存します。

---

### STEP 3: 初期セットアップ関数を実行する（シート自動生成）
1. エディタ上部のツールバーにある関数選択ドロップダウンから **`initialSetup`** を選択します。
2. **「実行」** ボタンをクリックします。
3. 初回実行時、「承認が必要です」というダイアログが表示されます：
   - **「アクセスを承認」** をクリック
   - ご自身の Google アカウントを選択
   - 「Google はこのアプリを検証していません」と表示されたら、左下の **「詳細」** をクリック
   - **「（プロジェクト名）に移動（安全ではないページ）」** をクリック
   - 画面下の **「許可」** をクリック
4. 実行ログに「`初期セットアップが完了しました！`」と表示されたら成功です！
5. スプレッドシートに戻ると、**「設定」「日程」「券種」「予約一覧」** の4つのシートが自動生成されています。

---

### STEP 4: Webアプリとして公開（デプロイ）する
1. Apps Script 画面右上の **「デプロイ」 > 「新しいデプロイ」** をクリックします。
2. 左側の歯車アイコン⚙️をクリックし、**「ウェブアプリ」** を選択します。
3. 各項目を以下のように設定します：
   - **説明**: `チケット予約システム v3 (個別チケット・n枚キャンセル対応)`（任意）
   - **次のユーザーとして実行**: `自分 (your-email@gmail.com)`
   - **アクセスできるユーザー**: `全員 (Anyone)` ⚠️必ず「全員」にしてください
4. **「デプロイ」** ボタンをクリックします。
5. 表示された **「ウェブアプリ URL」**（`https://script.google.com/macros/s/AKfy.../exec`）をコピーします。

---

### STEP 5: `ticket.html` に WebアプリURL を設定する
`ticket.html` 内の **1390行目付近** に、STEP 4 でコピーしたURLを貼り付けます：

```javascript
const GAS_ENDPOINT_URL = 'ここにコピーしたウェブアプリURLを貼り付け';
```

---

## 📝 各シートの構成と運用方法

### ① `設定` シート（公演設定・メール文面管理）
メール件名や本文、キャンセル機能のURLをここで自由に変更できます。

| 設定項目 | 設定値の例 | 説明 |
| :--- | :--- | :--- |
| **公演名** | 劇団ハレトケ 2026年 秋公演 | ページ見出し・メール内公演名 |
| **サブタイトル** | 〜いま、ここ、こそ、パラダイス〜 | キャッチコピー |
| **会場名** | 〇〇劇場 | 会場名 |
| **会場URL** | `https://example.com/venue` | 会場公式サイトやGoogleマップ等のURL（会場名の右にリンク表示されます） |
| **会場住所・アクセス** | 東京都〇〇区...（〇〇駅 徒歩5分） | アクセス情報 |
| **チケットページURL** | `https://corgiii-w7.github.io/Haretoke--web/ticket.html` | キャンセルURLの生成ベース |
| **販売ステータス** | `自動`（または `販売中` / `予約開始前` / `販売停止/終了`） | 自動判定または手動切替 |
| **販売開始日時** | `2026-09-01 12:00` | 全公演回共通の予約開始日時 |
| **公演説明・あらすじ** | 次回公演に向けて鋭意稽古中です！ | 公演のあらすじ |
| **注意事項・備考** | 開場は各回開演の30分前です... | チケット注意事項 |
| **自動返信メール送信** | `有効`（または `無効`） | 自動メール送信のON/OFF |
| **自動返信メール件名** | `【劇団ハレトケ】チケットご予約完了のお知らせ（予約番号: {予約番号}）` | 予約完了メールの件名 |
| **自動返信メール本文** | 自由記述（※下記プレースホルダーが使用可能） | 予約完了メールの本文 |
| **キャンセル通知メール件名**| `【劇団ハレトケ】チケットキャンセル手続き完了のお知らせ（予約番号: {予約番号}）` | キャンセル時の件名 |
| **キャンセル通知メール本文**| 自由記述（※下記プレースホルダーが使用可能） | キャンセル時の本文 |
| **チケット追加通知メール件名**| `【劇団ハレトケ】チケット追加予約完了のお知らせ（予約番号: {予約番号}）` | チケット追加時の件名 |
| **チケット追加通知メール本文**| 自由記述（※下記プレースホルダーが使用可能） | チケット追加時の本文 |
| **残りわずか判定割合(%)**| `20` | 残席がこの割合（%）以下になったら「残りわずか」表示にします（例：20） |
| **残りわずか判定枚数**| `5` | 残席がこの枚数以下になったら「残りわずか」表示にします（例：5） |

#### 📧 メール本文で使えるタグ（自動置換されます）
- `{お名前}` : 予約者のお名前（例: 山田 太郎）
- `{フリガナ}` : 予約者のフリガナ
- `{予約番号}` : 一意の予約番号（例: `HT-20261024-1234`）
- `{キャンセルURL}` : **お客様専用の確認・変更・キャンセルリンク**
- `{公演名}` : 設定シートの公演名
- `{公演日時}` : 予約された公演回の日時
- `{券種}` : 選択された券種
- `{枚数}` / `{有効枚数}` : 現在有効なチケット枚数
- `{追加枚数}` : 追加された枚数（追加通知時）
- `{キャンセル枚数}` : キャンセルされた枚数（キャンセル通知時）
- `{合計金額}` : 現在の有効合計金額
- `{知ったきっかけ}` : アンケート回答
- `{会場名}` : 会場名
- `{会場URL}` : 会場詳細URL
- `{注意事項}` : 注意事項・備考

---

### ② `日程` シート（公演日時・定員・販売終了日時）
各公演回ごとに「定員（上限席数）」と「販売終了日時」を設定できます。**定員を入力しておくと、予約が入るたびに残席が自動計算され、ステータスや選択可能枚数が自動制御されます。**

| 公演日時 | 残席ステータス | 定員/上限枚数 | 販売終了日時 | 備考/開場時間 |
| :--- | :--- | :--- | :--- | :--- |
| `2026年10月24日(土) 14:00` | `自動` | `40` | `2026-10-24 12:00` | 開場 13:30 |
| `2026年10月24日(土) 18:30` | `自動` | `40` | `2026-10-24 16:30` | 開場 18:00 |
| `2026年10月25日(日) 13:00` | `自動` | `40` | `2026-10-25 11:00` | 開場 12:30 |

> **💡 自動残席制御のポイント:**
> - **定員/上限枚数** に数値を入力しておくと、予約一覧シートの有効予約枚数を自動集計します。
> - 残り席数が20%以下（または5枚以下）になると、自動で **「残りわずか (残〇席)」** に切り替わります。
> - 残り席数が0になると、自動で **「受付終了」** に切り替わり、新規予約やチケット追加がブロックされます。
> - 画面上のチケット枚数選択でも、**残席数（例：残り2席なら最大2枚まで）に応じて選択肢が自動で制限**されます。
> - 特定の回を手動で止めたい場合は、残席ステータスに `販売停止` または `完売` と記入すれば即時停止できます。

---

### ③ `券種` シート（チケット種別と価格）
| 券種名 | 料金(円) | 説明・対象 |
| :--- | :--- | :--- |
| `一般` | `3000` | 一般前売券 |
| `U-25` | `2000` | 25歳以下対象（当日要身分証明書） |
| `応援プレミアム` | `4500` | 前方良席指定 ＋ オリジナル特典付き |

---

### ④ `予約一覧` シート（1行＝1チケット管理）
予約が入ると、**予約枚数分（例：5枚予約なら5行）が1行ずつ自動生成**され、各チケットに個別番号（`HT-20261024-1234-1`, `HT-20261024-1234-2`...）が割り振られます。

| 列 | 項目名 | 説明・例 |
| :--- | :--- | :--- |
| A | 予約日時 | 予約受付日時 |
| B | **予約番号** | 例: `HT-20261024-1234`（同一予約は共通） |
| C | **チケット番号** | 例: `HT-20261024-1234-1`, `HT-20261024-1234-2`（チケットごとの個別番号） |
| D | キャンセルキー | お客様ごとの一意の認証キー（例: `ck_abc123...`） |
| E | **ステータス** | `有効`（キャンセルされたチケットのみ自動で `キャンセル済み` に更新） |
| F | お名前 | 予約者名 |
| G | フリガナ | フリガナ |
| H | メールアドレス | 予約完了メール・キャンセル通知メールの送信先 |
| I | 公演日時 | 予約公演日時 |
| J | 券種 | 選択券種 |
| K | 金額(円) | チケット1枚あたりの単価 |
| L | 知ったきっかけ | アンケート回答 |
| M | 備考・メッセージ | お客様からの備考 |
| N | **キャンセル日時** | キャンセルが実行された日時 |

---

## 🔄 お客様による「n枚キャンセル（減数）」の流れ

1. **予約時**:
   - 予約完了メール内に `{キャンセルURL}` が届きます。
2. **枚数変更・キャンセル時**:
   - お客様がリンクをクリックすると、現在の予約状況（有効枚数・合計金額・各チケットの状況）が表示されます。
   - **複数枚予約している場合**：
     - ドロップダウンで「1枚をキャンセル（残り4枚にする）」「2枚をキャンセル...」「全枚数をキャンセル」から**キャンセルする枚数を選択**できます。
3. **実行後**:
   - スプレッドシート上で、指定された枚数分のチケット行のみが **`キャンセル済み`** に更新されます（残りの行は `有効` のまま維持）。
   - お客様宛に「〇枚のキャンセルを承りました。現在の有効枚数: 〇枚（合計: ¥〇〇）」と更新された確認メールが届きます。

---

## 📢 画面・メールに自動表示されるご案内事項

システム内（Web予約画面・予約完了メール・キャンセル画面・キャンセル通知メール）に以下を明記しています：

> 💡 **座席指定について**
> 「予約時に振り分けられる番号は、チケット管理のものであり、それによって座席を指定するものではございません。」

> 💡 **公演日時の変更について**
> 「公演時間の変更をご希望の場合は、一度チケットをキャンセルいただき、新しくご希望のチケットをご予約ください。」
